// factorial Recursion
#include <stdio.h>

int main() {
    fact(5,1);
    return 0;
}

int fact(int a, int n) {
    if (a <= 0) {
        printf("%d", n);
        return;
    }
    n = n * a;
    fact(a - 1, n);
    return n;
}

